# Step 1 Status

## Goal
Establish the parametric BOM and atomic proposal data contracts.

## Completion gate
- `python main.py` => PASS
- `pytest -q` => all tests pass
- The first verified engineering-data IDs can replace TEST_* values without changing the schema.
